# Entorno
venv/
.env
.env.*

# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / SO
.DS_Store
.vscode/
.idea/

# Corpus con datos reales — el corpus de eval/ debe ser 100% sintetico (ver PLAN.md TICKET-201).
# Cualquier archivo que contenga datos reales de una persona va aqui, nunca al repo.
eval/corpus/**/real/
*.real.json
*.real.csv

# Salida del arnes de medicion (TICKET-202): resultado transitorio de la
# ULTIMA corrida local, no un registro historico -- eso es docs/metrics-log.md,
# que si se comitea a mano cuando hay algo que reportar.
eval/harness/ultimo_resultado.json
eval/harness/ultimo_resultado.md
